services:

  frontend:
    build: ./frontend
    container_name: frontend-app
    ports:
      - "3000:80"

  backend:
    build: ./backend
    container_name: backend-api
    ports:
      - "5000:5000"
